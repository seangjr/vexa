# =============================================================================
# Vexa Lite - Consolidated Python Dependencies
# =============================================================================
# Merges dependencies from all Python services (api-gateway, admin-api,
# meeting-api, runtime-api, agent-api, tts-service) with resolved conflicts.
# =============================================================================

# Web Framework
fastapi>=0.128.8
uvicorn[standard]>=0.39.0
python-multipart>=0.0.20
starlette>=0.49.1

# Data Validation
pydantic[email]>=2.4.0
pyyaml>=6.0

# Database
SQLAlchemy[asyncio]>=2.0
psycopg2-binary>=2.9
asyncpg>=0.29
alembic>=1.13

# Redis
redis[hiredis]>=5.0

# HTTP Clients
httpx>=0.28.1
h11>=0.16.0                       # CVE-2025-43859 (httpx/uvicorn transitive)
idna>=3.15
requests>=2.32.5
requests-unixsocket>=0.3

# Object Storage (recordings)
boto3>=1.34

# WebSockets
websockets>=11.0

# Scheduling (runtime-api, agent-api)
croniter>=2.0

# TTS Service
piper-tts>=1.4.0
numpy>=1.22.0,<2.0.0
langdetect>=1.0.9

# Utilities
python-dotenv>=1.2.1
email-validator>=2.0

Mako>=1.3.12

# Observability — OpenTelemetry auto-instrumentation (lite ships OTLP/HTTP export)
# opentelemetry-bootstrap (run at build time) adds the matching instrumentors
# for the packages above (fastapi, requests, redis, sqlalchemy, psycopg2, httpx,
# urllib, logging). The OTLP/HTTP exporter targets OTEL_EXPORTER_OTLP_ENDPOINT.
opentelemetry-distro>=0.50b0
opentelemetry-exporter-otlp-proto-http>=1.29.0
